# Allow access to QR images
<IfModule mod_headers.c>
    Header set Access-Control-Allow-Origin "*"
</IfModule>

# Allow image files
<FilesMatch "\.(png|jpg|jpeg|gif|webp)$">
    Order allow,deny
    Allow from all
</FilesMatch>

